name: CI gates + Supabase migrations + Vercel deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production-main
  cancel-in-progress: false

jobs:
  test-migrate-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Harness and safety checks
        run: |
          python scripts/check_destructive_changes.py
          python scripts/check_agent_ownership.py lead
          python -m py_compile scripts/check_destructive_changes.py scripts/check_agent_ownership.py

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install Python dependencies
        run: pip install -r requirements.txt pytest

      - name: Backend verification
        run: bash scripts/verify_backend.sh

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Frontend verification
        run: bash scripts/verify_frontend.sh

      - name: Database verification
        run: bash scripts/verify_database.sh

      - name: Validate Supabase secrets
        if: github.event_name != 'pull_request'
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
        run: |
          test -n "$SUPABASE_ACCESS_TOKEN" || (echo "Falta SUPABASE_ACCESS_TOKEN" && exit 1)
          test -n "$SUPABASE_PROJECT_ID" || (echo "Falta SUPABASE_PROJECT_ID" && exit 1)

      - name: Apply pending Supabase migrations
        if: github.event_name != 'pull_request'
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
        run: python .github/scripts/apply_supabase_migrations.py

      - name: Check Vercel configuration
        if: github.event_name != 'pull_request'
        id: vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          if [ -n "$VERCEL_TOKEN" ] && [ -n "$VERCEL_ORG_ID" ] && [ -n "$VERCEL_PROJECT_ID" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
            echo "Vercel configurado: se desplegará producción."
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "Vercel no tiene las tres credenciales requeridas; se omite el deploy sin afectar pruebas ni migraciones."
          fi

      - name: Deploy production to Vercel
        if: github.event_name != 'pull_request' && steps.vercel.outputs.ready == 'true'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: npx vercel@latest deploy --prod --yes --token "$VERCEL_TOKEN"
