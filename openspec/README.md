# OpenSpec local

Este repositorio usa especificaciones versionadas, sin dependencia runtime. Las reglas estables viven en `specs/`; una propuesta concreta vive en `changes/<id>.md`, se implementa con escenarios Given/When/Then y se cierra sólo con verificación.
